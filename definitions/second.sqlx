config {
    type: 'table',
    description: "target table",
    name: 'second',
}

SELECT
  SUM(number) AS total
FROM
  ${ref("first")} AS Sum_Table
UNION ALL
SELECT
  *
FROM
  Sum_Table
