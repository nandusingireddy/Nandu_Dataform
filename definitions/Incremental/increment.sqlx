config {
    database: 'secure-potion-409606',
    schema: "Results",
    type: "incremental",
    name: "colors_increment",
    uniqueKey: ["id"]
    
}

SELECT
  *
FROM
  `secure-potion-409606.NanduSingireddy.colours` 
 ${when(incremental(), `WHERE timestamp > (SELECT MAX(timestamp) FROM ${self()})`)}